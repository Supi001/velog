<p>01_변수와 상수</p>
<pre><code class="language-JSX">
// 주석

변수와 상수 = 값을 저장하는 BOX 개념

변수 let은 값을 재할당 할수있는 변수

상수 const는 값을 재할당 할수없는 상수

둘다 변수를 선언하는 키워드

선언과 초기화

값을 저장하고 자신만의 이름을 가지는 특성을 가진다

// 변수 명명규칙(네이밍 규칙)

1. $, _ 를 제외한 기호는 사용할 수 없다.
2. 숫자로 시작하는 변수는 만들수없다

// 변수 명명 가이드

누가봐도 이해할수있는 변수명 필수
</code></pre>
<hr />
<p>02_자료형(타입)</p>
<pre><code class="language-jsx">자료형은 &quot;집합&quot;

자료형은 원시 타입, 객체 타입으로 나뉘어짐

// 숫자 타입 (Number)
기본적인 연산자를 이용한 계산 가능
숫자 아닌값은 NAN
나머지 구하는 모듈러 연산

// 문자 타입 (String)

문자와 문자를 이어붙이는 덧셈 연산 지원

let mySelf = `${동적값}은 ${동적값}에 거주합니다`
= 템플릿 리터럴 문법

// 참과 거짓 (boolean)

true or false

// 아무것도 없다 (null)

null 개발자가 의도적으로 비워두는값

// 찾을수없음 (undifined)

undifined 존재하지않는 값을 불러오려고 할때 발생하는 오류</code></pre>
<hr />
<p>03_형변환</p>
<pre><code class="language-jsx">// 형 변환

어떤 값의 타입을 다른 타입으로 변경

// 묵시적, 명시적 형 변환

묵시적 = JS 엔진이 바꾸는 강제값

명시적 = 내가 직접 바꾸는 값

숫자와 문자를 덧셈연산하면 숫자가 문자로 형변환 되듯이 에러 방지를 위한 묵시적 형변환</code></pre>
<hr />
<p>04_연산자</p>
<pre><code class="language-jsx">// 연산자

1. 대입 연산자 이퀄(=) 값을 할당하는 연산자

2. 산술 연산자 + - * / %(나머지,모듈러)

3. 복합 대입 연산자 ( 산술과 대입이 복합되어있다 )
+= -= (이퀄 사인 앞에는 모든 산술연산자가 올 수 있음)

4. 증감 연산자
++ -- 값의 앞뒤에 오는것에 따라 달라짐
값의 앞에 붙이면 값의 라인에서 즉시 연산 ++12 = 즉시 13
값의 뒤에 붙이면 값의 다음 라인부터 연산 12++ = 12 다음 13

5. 논리 연산자
or (혹은) 연산자 = true || false 왼쪽이 참이면 그대로 반환, 아닐시 오른쪽 반환

and (그리고) 연산자 = true &amp;&amp; true 둘 다 참이어야만 참
실제로는 왼쪽이 true면 
5 &amp;&amp; 10 일때 // 10

오른쪽 반환 false면 왼쪽 반환
0 &amp;&amp; 10 일때 // (0은 거짓이기 때문에)

not 연산자 = true 는 false false 는 true 로 반전시킴

6. 비교 연산자
값 = 1 === 2 거짓 (==는 자료형 검사는 안하기때문에 사용X)
값 = 1 !== 2; 참

&lt; &gt; &gt;= =&gt; 크거나 작거나 크거나 같다 작거나 같다까지 대소 비교가능
값 = 1&lt;2

7. 널 병합 연산자
실제로 존재하는 값을 추려내는 기능
null, undifined가 아닌 값 반환

null ?? user = user가 출력

8. typeof
값의 타입을 판별해서 반환해줌

9. 삼항 연산자
항을 3개 사용하는 연산자

조건식 참 거짓 순으로 3항이 이루어지며
let exam = var1 % 2 === 0 ? &quot;짝수&quot; : &quot;홀수&quot;;

풀어보면 exam이라는 변수는 var1이라는 값이 2로 나눴을때 나머지가 0이라면 짝수 아니라면 홀수를 출력

물음표 이전 var1 ~ 0 까지가 조건식

물음표 이후가 참, 콜론뒤가 거짓

exam은 말 그대로 Box역할이다 마치 완성된 요리 같은 개념 완성된 결과를 담아내는

var1은 재료 같은개념 값이다 말그대로

그 뒤 조건식은 요리법 같은 개념이다 값에 어떤 적용을 하는</code></pre>