name: Update Blog Posts

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'   # UTC 매일 00:00 → KST 매일 09:00
    - cron: '0 15 * * *'  # UTC 매일 15:00 → KST 매일 00:00

jobs:
  update_blog:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install feedparser gitpython

      - name: Run update_blog.py
        run: python scripts/update_blog.py

      - name: Configure Git and Push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "Update blog posts from Velog" || echo "No changes to commit"
          git push https://${{ secrets.GH_PAT }}@github.com/Supi001/velog.git
